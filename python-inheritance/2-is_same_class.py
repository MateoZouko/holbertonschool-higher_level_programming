#!/usr/bin/python3
"""
function is same class
"""


def is_same_class(obj, a_class):
    """
    return true if type(obj) is a class
    if not return False
    """
    if type(obj) is a_class:
        return True
    else:
        return False
